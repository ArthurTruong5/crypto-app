<h1>Hello World</h1>
<p>This is my first Rails application</p>

<!-- THE = sign is only needed if you want to output something to the screen -->
<% @coins %>

  <% for x in @coins %>
    <% for coin in @my_coins %>
      <% if coin == x["symbol"] %>
      Rank <%= x["rank"] %> -
        <%= x["name"] %> : $<%= x["price_usd"] %><br><br>
      <% end %>
    <% end %>
  <% end %>
